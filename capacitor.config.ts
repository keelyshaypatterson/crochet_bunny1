import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.crochet_bunny1',
  appName: 'crochet_bunny1',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
